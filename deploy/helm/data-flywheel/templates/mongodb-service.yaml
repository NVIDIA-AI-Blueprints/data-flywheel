---
{{- if .Values.mongodb.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mongodb.fullnameOverride }}-service
spec:
  selector:
    app: {{ .Values.mongodb.fullnameOverride }}-deployment
  type: {{ include "data-flywheel.serviceType" . }}
  ports:
    - port: {{ .Values.mongodb.service.port }}
{{- end }}