---
{{- if .Values.elasticsearch.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.elasticsearch.fullnameOverride }}-deployment
  labels:
    app: {{ .Values.elasticsearch.fullnameOverride }}-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.elasticsearch.fullnameOverride }}-deployment
  template:
    metadata:
      labels:
        app: {{ .Values.elasticsearch.fullnameOverride }}-deployment
    spec:
      automountServiceAccountToken: false
      {{- if .Values.openshift.enabled }}
      securityContext:
        {{- include "data-flywheel.podSecurityContext" . | nindent 8 }}
      {{- end }}
      containers:
        - name: elasticsearch
          image: "{{ .Values.elasticsearch.image.repository }}:{{ .Values.elasticsearch.image.tag }}"
          imagePullPolicy: Always
          {{- if .Values.openshift.enabled }}
          securityContext:
            {{- include "data-flywheel.containerSecurityContext" . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.elasticsearch.resources | nindent 12 }}
          ports:
            - containerPort: {{ .Values.elasticsearch.service.port }}
          env:
            {{- range $key, $val := .Values.elasticsearch.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
{{- end }}
