---
{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.redis.fullnameOverride }}-service
spec:
  selector:
    app: {{ .Values.redis.fullnameOverride }}-deployment
  type: {{ include "data-flywheel.serviceType" . }}
  ports:
    - port: {{ .Values.redis.service.port }}
{{- end }}